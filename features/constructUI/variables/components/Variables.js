import styles from "./Variables.module.css"

export default function Variables() {
    return (
        <div className={styles.container}>
            <button className={styles.containerElement}>add variables</button>
        </div>
    )
}